import { Routes, Route } from "react-router-dom";

// page routes
import Home from "./pages/Home";

const App = () => {
  return (
    /* navbar */
    /* navbar */

    /* routes */
    <Routes>
      <Route path="/" element={<Home />} />
    </Routes>
    /* routes */

    /* footer */
    /* footer */
  );
};

export default App;
